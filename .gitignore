node_modules/
.env
.env.local
*.db
*.sqlite
logs/*.log
data/*.db
data/*.sqlite
.DS_Store
coverage/
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
data/*.sqlite-*
data/*.bak

# Runtime strategy state (non-secret, but local/per-run — committed template is
# config/strategy-settings.example.json). The bot may write this only with --write.
data/strategy-settings.json
data/*.local.json
